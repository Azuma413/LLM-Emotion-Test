experiment_name: llm-emotion-test-sft
stage: sft
model:
  base_model: Qwen/Qwen2.5-0.5B-Instruct
soft_prompt:
  num_latents: 8
  prompt_length: 4
data:
  dataset_name: shunk031/wrime
  dataset_config: ver1
  processed_filename: sft-smoke.jsonl
  text_column: sentence
  annotation_source: writer
  max_samples: 100
training:
  learning_rate: 0.00002
  batch_size: 1
  max_steps: 10
  report_to: wandb
output:
  root: outputs/runs
  run_id: sft-smoke
